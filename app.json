{
  "name": "ember-api-docs",
  "scripts": {
  },
  "env": {
    "FASTLY_API_KEY": {
      "required": false
    },
    "FASTLY_SERVICE_ID": {
      "required": false
    },
    "HEROKU_EMBER_CLI_DEPLOY": {
      "required": false
    },
    "WORKER_COUNT": {
      "required": false
    }
  },
  "formation": {
  },
  "addons": [
  ],
  "buildpacks": [
    {
      "url": "https://buildpack-registry.s3.amazonaws.com/buildpacks/aedev/emberjs.tgz"
    }
  ]
}
