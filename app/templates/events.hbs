<EmberAnchor @a={{anchor}} />
<ApiIndexFilter @model={{model}} @filterData={{filterData}} as |filteredModel|>
  {{#each filteredModel.events as |event|}}
    <ClassFieldDescription @type="event" @field={{event}} @model={{model}} />
  {{/each}}
</ApiIndexFilter>
