{{yield (hash
  sections=sections
  projectId=itemData.project.id
  projectVersion=itemData.projectVersion.version
  name=itemData.name
  showInherited=showInherited
  showProtected=showProtected
  showPrivate=showPrivate
  showDeprecated=showDeprecated)}}

