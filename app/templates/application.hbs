{{! template-lint-disable no-implicit-this }}
{{page-title "Ember Api Docs"}}
<EsHeader>
  <EsNavbar @links={{links}}>
    <SearchInput />
  </EsNavbar>
</EsHeader>
<main class="container">
  {{outlet}}
</main>
<EsFooter />

{{!-- required by ember-cli-meta-tags --}}
<HeadLayout />
